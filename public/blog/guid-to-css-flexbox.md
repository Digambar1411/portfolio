#Basics and terminology

Flexbox is a way to arrange items in one-dimensional layout method, either in rows or columns. Since flexbox is whole module and not single property, it has whole set of properties. Some of theme are meant to be set on container (parent element, flex-container ) whereas others are meant to be set on items (child elements, flex-items).